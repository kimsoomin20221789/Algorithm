#include <bits/stdc++.h>
using namespace std;

int main() {
    int N; cin >> N;
    string name[N];
    int korea_score[N];
    int english_score[N];
    int math_score[N];

    for (int i=0; i<N; i++) {
        cin >> name[i] >> korea_score[i] >> english_score[i] >> math_score[i];
    }
}